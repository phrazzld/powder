# Convex Backend

This directory contains the Convex backend functions.

To initialize Convex:
1. Run `pnpm convex dev`
2. Follow the prompts to create a new project
3. Copy the NEXT_PUBLIC_CONVEX_URL to .env.local
